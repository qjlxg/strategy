name: Stock Scanner Backtest Go Execution

on:
  workflow_dispatch:
  push:
    paths:
      - 'stock_scanner_go.py'
      - 'backtest_stock_scanner_go.py'
      - '.github/workflows/backtest_stock_scanner_go.yml'

jobs:
  run-backtest:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0 

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install Dependencies
        run: pip install pandas numpy pytz

      - name: Run Backtest
        env:
          TZ: 'Asia/Shanghai'
        run: |
          mkdir -p results
          python backtest_stock_scanner_go.py

      - name: Commit and Push Results
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          # 处理冲突：先暂存本地回测结果，拉取远程，再恢复并提交
          git add results/*.csv
          git stash
          git pull --rebase origin main
          git stash pop || echo "No stash to pop"
          
          git add results/*.csv
          git diff --quiet && git diff --staged --quiet || (git commit -m "📈 优化版回测报告推送 [$(date '+%Y-%m-%d %H:%M')]" && git push origin main)
