name: Stock Scanner Backtest Go Execution

on:
  workflow_dispatch:
  push:
    paths:
      - 'stock_scanner_go.py'
      - 'backtest_stock_scanner_go.py'
      - '.github/workflows/backtest_stock_scanner_go.yml'

jobs:
  run-backtest:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0 # 必须获取完整历史以支持 rebase

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install Dependencies
        run: pip install pandas numpy pytz

      - name: Run Backtest
        env:
          TZ: 'Asia/Shanghai'
        run: |
          mkdir -p results
          python backtest_stock_scanner_go.py

      - name: Commit and Push Results
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add results/backtest_advanced_summary.csv
          
          # 关键点：先拉取远程更新并变基，解决推送冲突
          git pull --rebase origin main
          
          git diff --quiet && git diff --staged --quiet || (git commit -m "📊 自动更新回测报告 [$(date '+%Y-%m-%d %H:%M')]" && git push origin main)
